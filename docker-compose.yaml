version: '3.8'
services:
  cpuminer-rkz:
    build:
      context: .
      args:
        COIN: "cpuchain"
    image: "innovanon/cpuminer-rkz:${DOCKER_TAG}"
    deploy:
      mode: replicated
      replicas: 1
      #update-delay: 10s
      placement:
        constraints: [node.role == worker]

